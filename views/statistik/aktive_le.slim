a.btn.btn-default href="#{url('/statistik')}"== "#{glyphicon 'chevron-left'} zur&uuml;ck"

.page-header
  h1
    | Aktive Spitäler
    small< StammSpital

p.lead Liste aller aktiven Spitäler

h2
  = "#{@rows.count} Datensätze"
  a.btn.btn-success< href="#{url('/statistik/aktive_le.xlsx')}" Download für Excel

table.table.table-striped.table-hover
  thead
    tr== @cols.map { |c| "<th>#{c.capitalize}</th>" }.join
  tbody
    - @rows.each do |row|
      tr== row.values.map { |v| "<td>#{v}</td>" }.join
